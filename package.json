{
  "name": "loanlab",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @loanlab/web",
    "build": "npm run build --workspace @loanlab/web",
    "lint": "npm run lint --workspace @loanlab/web",
    "ios": "npm run ios --workspace @loanlab/mobile",
    "start": "npm run start --workspace @loanlab/mobile",
    "test": "npm run test --workspaces --if-present"
  }
}
